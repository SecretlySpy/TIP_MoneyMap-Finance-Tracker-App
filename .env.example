# Optional. Required only for online Smart Tips (FR-10b).
# Prefer EAS secrets for release builds. Never commit a real key.
# Exposed to the app via app.config.js → expo.extra.geminiApiKey
GEMINI_API_KEY=
