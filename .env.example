# Optional. Enables online Smart Tips (FR-10b) via Gemini when set.
# Smart Tips is implemented; without this key the screen stays offline-only.
# Prefer EAS secrets for release builds. Never commit a real key.
# Exposed to the app via app.config.js → expo.extra.geminiApiKey
GEMINI_API_KEY=
